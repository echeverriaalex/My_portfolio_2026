
  # Crear portafolio web

  This is a code bundle for Crear portafolio web. The original project is available at https://www.figma.com/design/7LxtDfQtH05MiC0OV5IAz1/Crear-portafolio-web.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  